spring:
  application:
    name: dariospainting

  datasource:
    url: jdbc:mysql://localhost:3306/dariospainting
    username: root
    password: Matt0038!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.gmail.com
    port: 587
    username: galvezmatthew135@gmail.com
    password: Matt0038
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
okta:
  oauth2:
    issuer: https://dev-6jd2lsz1hkxagd33.us.auth0.com/
    client-id: ${OKTA_CLIENT_ID} 
    client-secret: ${OKTA_CLIENT_SECRET} 

logging:
    level:
     org.springframework.mail: DEBUG
     org.springframework.mail.javamail: DEBUG
aws:
  s3:
    secret:
      key: ${AWS_SECRET_KEY} 
    access:
      key: ${AWS_ACCESS_KEY} 



bucket:
  name: dariospainting-images

